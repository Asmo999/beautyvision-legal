{
  "name": "beautyvision-legal",
  "version": "1.0.0",
  "private": true,
  "description": "Hosted legal pages for Beauty Vision (Privacy Policy)",
  "scripts": {
    "build": "npm --prefix Admin_Beauty_Vision run build",
    "build:vercel": "npm --prefix Admin_Beauty_Vision ci --include=dev --no-audit --no-fund && npm --prefix Admin_Beauty_Vision run build && node scripts/copy-admin-dist.mjs",
    "setup": "npm --prefix Admin_Beauty_Vision install --no-audit --no-fund",
    "dev": "npm run build && node server.mjs",
    "start": "node server.mjs"
  }
}
